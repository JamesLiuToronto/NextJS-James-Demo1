
const getBaseUrl = (): string => {
    // You can extend this function to get the base URL from other sources
    // such as environment variables or configuration files
    return 'http://localhost:3001';
  };
  
  export default getBaseUrl;
